# Virtual environments
venv/
venv.broken/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# macOS
.DS_Store

# Accumulator test harness — large regenerable outputs
# (CSVs, PNGs, and summary.json are kept; events.json / results.json / sweep/
# are regenerable via analysis/accumulator_test/run_all.sh)
analysis/accumulator_test/runs/**/events.json
analysis/accumulator_test/runs/**/results.json
analysis/accumulator_test/runs/sweep/
